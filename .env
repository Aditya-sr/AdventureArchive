APP_NAME=Laravel
APP_ENV=production  # Change this to 'production' for live deployment
APP_KEY=base64:eR1f+/uY+HYXhQtFj1H/PcbV27U8QE4aDtyX2un5Eo4=
APP_DEBUG=false  # Set to false in production for security
APP_URL=https://adventurearchive-production.up.railway.app  # Use the hosted URL here

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1  # Railway will provide the correct DB host URL
DB_PORT=3306
DB_DATABASE=adventure_archive
DB_USERNAME=root
DB_PASSWORD=  # Add the correct password or use Railway environment variables

BROADCAST_DRIVER=log  # If not using broadcasting, leave as 'log'
CACHE_DRIVER=file  # Or 'redis' if using Redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file  # Or 'redis' if using Redis
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1  # Set if using Memcached, else remove

REDIS_HOST=127.0.0.1  # Use the provided Redis URL if using Redis
REDIS_PASSWORD=null  # Set this if Redis requires a password
REDIS_PORT=6379

# Removed Mail and Pusher configuration as you're not using them

# AWS_ACCESS_KEY_ID=  # Remove if you're not using AWS services
# AWS_SECRET_ACCESS_KEY=  # Remove if you're not using AWS services
# AWS_BUCKET=  # Remove if you're not using AWS S3
